doctype html
html
    head
        title Express-app 3
        script(src='index.js')
        style 
            include index.css
    body
        form(action="/addPerson" method="post")
            input#navn(type="text" name="navn")
            input#adresse(type="text" name="adresse")
            button#submit(type="submit") Tilføj
            //- #submit^ i en 'form' behøver ikke en addMetode; i linje 9 kobles 'method post' med 'action addPerson'
            //- obs på at metoden addPerson hedder præcis det samme i post-metoden i app.js-filen
        select#personer(name="" multiple)
            each p of personer
                option(value="")= p.navn + " " + p.adresse

